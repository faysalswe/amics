export interface pmSearch {
    partNumber: string;
    description: string;
    itemType: string;
    itemClass: string;
    itemCode: string;
}

export interface pmSearchResult {
    partNumber: string;
    rev: string;
    description: string;
}